import type { FC } from 'react';

export const BatchTrackerPage: FC = () => (
  <section className="card">
    <h2>Batch Tracker</h2>
    <p className="card-note">Batch-level progression is surfaced in QA Release Board and Dossier Viewer workflows.</p>
  </section>
);
