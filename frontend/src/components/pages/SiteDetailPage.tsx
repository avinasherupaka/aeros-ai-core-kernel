import type { FC } from 'react';

export const SiteDetailPage: FC = () => (
  <section className="card">
    <h2>Site Detail</h2>
    <p className="card-note">Site-detail drilldown is represented in the topology workspace for this release.</p>
  </section>
);
