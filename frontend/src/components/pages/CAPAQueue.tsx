import type { FC } from 'react';

export const CAPAQueue: FC = () => (
  <section className="card">
    <h2>CAPA Queue</h2>
    <p className="card-note">CAPA queue is integrated in the QA Release Board for consolidated triage.</p>
  </section>
);
