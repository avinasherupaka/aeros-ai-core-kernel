import type { FC } from 'react';

import type { StatusBadgeProps } from '../../types/control-plane';

/**
 * Shell component for displaying normalized control-plane status as a traffic light badge.
 */
export const TrafficLightBadge: FC<StatusBadgeProps> = (_props) => null;
