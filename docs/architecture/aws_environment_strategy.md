# AWS Environment Strategy

- Dev: shared engineering sandbox.
- QA: controlled integration validation.
- Prod cell template: copied per customer or site pattern.

Default region guidance: `ap-south-1` for India residency needs (configurable).

Isolation strategy can evolve from logical tenant isolation to account-level isolation.
