# Tenant A — Aurora Pharma (local development)
# Selects the facility served by the core. Copy/rename to add more tenants.
AREOS_TENANT=pharma_co_a
AREOS_TENANTS_DIR=/tenants
AREOS_MODE=local_mock
ARTIFACTS_ROOT=/data/artifacts
